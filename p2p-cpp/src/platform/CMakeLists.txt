# Platform Abstraction Layer (Placeholder)
# TODO: Implement platform-specific code

# Placeholder library
add_library(p2p_platform INTERFACE)
target_include_directories(p2p_platform INTERFACE
    ${CMAKE_SOURCE_DIR}/include
)
