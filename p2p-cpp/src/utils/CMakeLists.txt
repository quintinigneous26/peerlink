# Utils Module (Placeholder)
# TODO: Implement logging, config, thread pool, etc.

# Placeholder library
add_library(p2p_utils INTERFACE)
target_include_directories(p2p_utils INTERFACE
    ${CMAKE_SOURCE_DIR}/include
)
